apply plugin: "war"

bootWar {
    mainClassName = "com.jhipster.demo.dummy.DummyApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
